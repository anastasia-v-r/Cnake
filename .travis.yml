# Enable OS
os: windows

# Enable Lang
language: cpp

# Enable Compilers
compiler:
  - gcc
  - clang

# Pre-Install
before_install:
  - echo $PATH
  - choco install conan
  - mkdir conan
  - cd conan
  - conan install ..

# Install
install:
  - echo $PATH

# Setup Build
before_script:
  - cd ..
  - mkdir build
  - cd build
  - cmake ..
  
# Build and Test
script:
  - make && make test
