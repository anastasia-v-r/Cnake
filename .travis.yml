# Enable OS
os: windows

# Enable Lang
language: cpp

# Enable Compilers
compiler:
  - gcc
  - clang

# Pre-Install
before_install:
  - echo $PATH
  - choco install conan
  - echo %PATH%

# Install
install:
  - mkdir conan
  - cd conan
  - conan install ..

# Setup Build
before_script:
  - cd ..
  - mkdir build
  - cd build
  - cmake ..
  
# Build and Test
script:
  - make && make test
